# Outlook Mail Manager 代码审查报告

## 1. 审查结论

- 项目：`czppw/outlook-mail-manager`
- 审查基线：`fcc58e9f8b09b0d046da4f1bdd7bc29495728973`
- 目标版本：`v1.0.0`
- 审查日期：2026-07-27
- 项目定位：个人自托管邮箱管理工具，默认单用户、单进程运行

项目原有主要功能已经完整，包括账号导入、Microsoft Graph/IMAP 取件、Gmail IMAP、代理、邮件查看、健康检查和令牌状态管理。本次没有重写业务流程，审查重点是修复会直接导致功能失败、凭据泄露、页面被邮件内容攻击，或自动更新损坏本地数据的问题。

当前版本可以作为个人项目发布。核心功能、数据库迁移、Web 工作流和自动更新器均有自动化测试；真实 Microsoft 测试账号的 Graph 刷新和取件也已通过。发布前唯一必须完成的事项是为自动更新器写入正式 Ed25519 公钥，并用对应私钥生成签名清单。

## 2. 代码结构

| 模块 | 作用 | 审查评价 |
|---|---|---|
| `app.py` | FastAPI 路由、登录、账号操作、取件调度、更新 API | 功能集中但路径清楚；对个人项目无需继续拆分 |
| `db.py` | SQLite 建表、V1-V6 迁移、会话、账号和邮件数据 | 迁移和事务覆盖较完整；保留单库设计合适 |
| `mail_fetcher.py` | OAuth2、Graph、IMAP、SOCKS 代理和 MIME 解析 | 主要协议错误已修复；保留旧调用接口兼容 |
| `email_sanitizer.py` | 邮件 HTML 白名单清洗 | 独立且职责明确 |
| `security.py` | Fernet 字段加密和密钥加载 | 实现已简化；没有引入 Windows 原生 ACL 状态机 |
| `update_manager.py` | GitHub Release 检查、签名验证、下载和事务更新 | 代码量较大，但这是保护本地数据所需的主要复杂度 |
| `launcher.py` | 选择当前/版本化 venv 并启动应用 | 已收敛为轻量启动器，不承担第二套回滚状态机 |
| `web_security.py` | 请求体限制和常用响应头 | 小型 ASGI 中间件，未引入额外框架 |

## 3. 主要发现与处理

### 3.1 直接影响功能的问题

| 级别 | 基线问题 | 影响 | 当前处理 |
|---|---|---|---|
| 高 | XOAUTH2 回调内容被重复 Base64 | IMAP 登录会失败 | 回调返回原始 SASL bytes，由 `imaplib` 编码一次 |
| 高 | IMAP 使用消息序号而不是稳定 UID | 邮箱变化后可能漏信或错误去重 | 使用 UID SEARCH/FETCH，并以 `UIDVALIDITY:UID` 保存 |
| 中 | PySocks 参数和 DNS 模式不正确 | SOCKS 代理不可用或发生本地 DNS 解析 | 修正参数，区分 `socks5` 与 `socks5h` |
| 中 | Graph 对 429/408/5xx 直接失败 | 临时限流被误判为账号异常 | 仅对幂等 GET 做有限重试并支持 `Retry-After` |
| 高 | refresh token 轮换后保存时机不可靠 | 取件失败时可能丢失供应商返回的新 token | 先刷新并用 CAS 保存新 token，再执行取件 |
| 中 | 空 refresh token 可能覆盖有效值 | 账号永久失联 | 只有非空轮换 token 才更新数据库 |
| 中 | 页面清空代理后仍可能继承环境代理 | 用户无法明确选择直连 | `None` 表示允许环境回退，空字符串表示明确直连 |
| 中 | 单封异常/超大 MIME 邮件影响整次取件 | 有效邮件无法保存或账号状态误报 | 10 MiB 前置限制，保留有效部分并报告部分失败 |
| 中 | 最新一轮改动删除了旧取件接口 | 原有 `smoke_test.py` 和外部脚本失败 | 恢复 `_fetch_via_graph`、`fetch_all_emails` 等兼容入口 |

### 3.2 凭据和数据库

| 级别 | 基线问题 | 影响 | 当前处理 |
|---|---|---|---|
| 高 | refresh token、client secret、邮箱密码和代理明文存储 | 数据库副本可直接接管邮箱或代理 | 使用 Fernet 对敏感字段加密，启动时迁移旧数据 |
| 高 | 密钥丢失时可能生成新密钥 | 新旧密文混用，旧凭据无法恢复 | 加密 canary 校验；存在密文但密钥错误时拒绝启动 |
| 中 | 管理员密码使用一次 SHA-256 | 数据库泄露后容易离线破解 | 使用带随机盐的 scrypt，并兼容升级旧哈希 |
| 高 | 数据库迁移由零散 ALTER 组成 | 中断后可能形成半迁移状态 | 使用 `PRAGMA user_version` 和事务化 V1-V6 迁移 |
| 中 | SQLite 连接配置不一致 | 外键失效、锁冲突和孤儿数据 | 统一启用 foreign keys、WAL 和 busy timeout |
| 中 | 邮箱地址大小写可重复导入 | 同一账号出现多份记录 | 统一小写并使用 NOCASE 唯一索引 |
| 低 | 导入错误回显原始行 | 页面可能显示密码或 token | 只返回行号和脱敏错误类型 |

Microsoft 导入格式中的第二字段不参与 OAuth2 认证，当前不再保存历史邮箱密码。Gmail 第二字段仍作为 `client_secret` 使用。

### 3.3 Web 和邮件显示

| 级别 | 基线问题 | 影响 | 当前处理 |
|---|---|---|---|
| 高 | 会话状态仅在进程内保存且缺少服务端撤销 | 重启丢会话，改密后旧会话可能继续使用 | 随机 session token，数据库仅保存哈希，支持过期和撤销 |
| 高 | 状态修改接口缺少 CSRF | 第三方页面可触发导入、删除、改密或取件 | 修改请求校验 session CSRF 和同源 Origin |
| 中 | GET 导出和注销 | 容易被跨站触发或缓存 | 改为 POST；导出还要求当前管理员密码 |
| 中 | 登录失败锁定逻辑无效 | 暴力尝试不受限制 | 5 次失败后临时返回 429；测试覆盖该路径 |
| 中 | 请求体和导入行数无限制 | 大输入可耗尽内存或数据库 | 默认请求体 2 MiB，导入最多 10,000 行 |
| 严重 | 邮件 HTML 直接进入后台页面 | 可执行脚本、加载追踪像素或操作会话 | Bleach 白名单清洗，并放入无权限 sandbox iframe |
| 中 | 动态邮件列表使用 HTML 拼接 | 发件人或主题可能造成 DOM XSS | 使用 DOM API 和 `textContent` |

页面改密规则按项目要求保留为：至少 8 位，并同时包含 ASCII 字母和数字。新数据库默认账号仍为 `admin/admin123`。

### 3.4 自动更新

自动更新是本次唯一新增的主要功能。登录后版本框调用 GitHub `releases/latest`；检查、下载和依赖安装都使用页面保存的全局代理。

更新器只允许修改签名清单声明的仓库文件，并执行以下检查：

1. Release 版本必须严格高于当前版本。
2. 下载 URL 必须是 HTTPS，最终主机必须属于允许的 GitHub 域名。
3. `UPDATE_MANIFEST.json` 必须通过内置 Ed25519 公钥验证。
4. 每个文件的路径、大小和 SHA-256 必须与清单一致。
5. ZIP 中的绝对路径、`..`、符号链接、设备文件、重复路径和超限内容会被拒绝。
6. 更新前创建事务备份；中断后下次启动恢复未完成事务。
7. 依赖变化时使用 `requirements.lock --require-hashes` 创建 `.omm-venvs/<version>`，不修改现有 `venv/.venv`。
8. 未知本地文件不会被覆盖或删除。

明确不属于更新目标的内容：

- `data.db`、WAL/SHM 和数据库备份；
- `.omm_secret.key`、PEM/P12/PFX/JKS 等密钥；
- `.env` 和部署环境文件；
- 现有 `venv`、`.venv`；
- 日志、`.git`、缓存和其他未知本地文件。

启动器已从复杂的二次回滚状态机收敛为约 65 行，只负责读取 `.omm-runtime.json`、选择 Python 和启动 `app.py`。更新事务恢复继续由 `update_manager.py` 单点负责。

## 4. 验证结果

| 检查 | 结果 |
|---|---|
| Ruff | 通过，0 个错误 |
| `compileall` | 通过 |
| pytest | `74 passed, 25 subtests passed` |
| 独立 smoke test | 全部通过，包括 Graph 映射、数据库、迁移和真实 HTTP 流程 |
| Bandit | 0 高危；4 个中危均为固定 SQL 片段/占位符数量拼接，不包含用户 SQL |
| pip-audit | `No known vulnerabilities found` |
| 仓库敏感信息扫描 | 未发现 GitHub token、私钥、测试服务器地址或测试邮箱凭据 |
| 真实邮箱最小验证 | Microsoft；2 个目标文件夹；Graph 成功；refresh token 已轮换并写回测试文件 |

Bandit 还报告了低危提示，包括项目明确保留的 `admin123`、SQL 列定义字符串，以及更新器调用固定参数的 `pip`/`execv`。这些不属于可利用漏洞。

## 5. 保留风险

以下风险对个人项目可以接受，不建议继续扩展架构处理：

1. `admin123` 是公开默认密码。首次启动后应在页面修改；在改密前只监听 `127.0.0.1`。
2. Windows 上密钥文件依赖当前用户目录权限，没有加入 `ctypes` DACL 管理。个人单用户机器可接受；数据库和 `.omm_secret.key` 必须一起备份。
3. 应用按单进程使用设计。不要配置多个 Uvicorn worker；个人部署没有必要引入分布式任务调度。
4. `app.py` 和 `db.py` 文件较大，但目前业务耦合简单且测试覆盖主要路径。为拆文件而拆文件会增加维护成本，本次不重构。
5. Graph/IMAP 行为仍受 Microsoft、Google 的授权范围和 token 策略影响。供应商策略变化时可能需要调整 scope。
6. 自动更新私钥是更新信任根，只应保存在仓库外。私钥丢失后只能通过手工发布内置新公钥的版本恢复。
7. 本次没有连接或修改测试服务器，也没有验证 Nginx、证书、systemd 和备份恢复。这些属于部署验证，不影响代码审查结论。

## 6. 发布判断

代码已达到个人项目 `v1.0.0` 的发布标准。发布顺序应为：

1. 在仓库外生成 Ed25519 发布私钥。
2. 把对应公钥写入 `update_manager.py`。
3. 暂存最终文件并生成签名 `UPDATE_MANIFEST.json`。
4. 再次运行 Ruff、pytest、smoke test、Bandit 和 pip-audit。
5. 提交、创建 `v1.0.0` tag，并创建正式 GitHub Release。
6. 用公开 `releases/latest` 和登录后版本框验证发布结果。
